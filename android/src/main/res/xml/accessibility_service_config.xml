<?xml version="1.0" encoding="utf-8"?>
<!--
  Appcycle overlay trigger: when the user taps the accessibility button (or uses
  the accessibility shortcut), the service starts the main Activity with openOverlay=true.
  Minimal event types so we only react to the button callback, not to window events.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/appcycle_accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="false" />
